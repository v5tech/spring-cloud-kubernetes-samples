spring:
  config:
    activate:
      on-profile: kubernetes
    import: optional:configserver:http://spring-cloud-kubernetes-configserver:8888
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    kubernetes:
      enabled: true
      loadbalancer:
        mode: SERVICE
      discovery:
        # 开启服务注册发现
        enabled: true
        # 允许访问所有namespaces
        all-namespaces: true
        include-not-ready-addresses: false
        discovery-server-url: http://spring-cloud-kubernetes-discoveryserver

#feign:
#  client:
#    config-sample-url: http://spring-cloud-kubernetes-config-sample:8080