spring:
  application:
    name: svc-discovery-sample
  cloud:
    kubernetes:
      discovery:
        all-namespaces: true
        include-not-ready-addresses: true
      config:
        name: ${spring.application.name}
        namespace: default
server:
  port: 9080
